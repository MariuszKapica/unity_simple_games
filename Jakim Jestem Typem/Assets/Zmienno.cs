using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Zmienno : MonoBehaviour
{
    public float Liczba;
    // Start is called before the first frame update
    void Start()
    {
        Liczba = 6.6f;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
