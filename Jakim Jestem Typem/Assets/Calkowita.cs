using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Calkowita : MonoBehaviour
{
    public int intem;
    public float Liczba;
    

    // Start is called before the first frame update
    void Start()
    {
        intem = 3;
        Liczba = (float)intem;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
